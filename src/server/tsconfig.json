{
	"extends": "../../tsconfig.node.json",
	"compilerOptions": {
		// Rescope to `./*` (this dir contents and below)
		"rootDir": "./",
		"baseUrl": "./"
	},
	"include": [
		// Rescope to `./*` (this dir contents and below)
		".*/**/*",
		"*/**/*",
		".*",
		"*"
	],
	"$schema": "https://json.schemastore.org/tsconfig"
}