{
    "extends": "../../tsconfig.node.json",
    "include": [
        // Only from **this** dir and below
        ".*/**/*",
        "*/**/*",
        ".*",
        "*"
    ],
    "$schema": "https://json.schemastore.org/tsconfig"
}